---
phase: 02-security-shell-execution
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03, EXEC-01, EXEC-02]

must_haves:
  truths:
    - "Safe command (ls -la ~/) runs without approval and returns directory listing"
    - "Risky command (npm install some-pkg) triggers inline keyboard"
    - "Blocked command (rm -rf /) returns block explanation without executing"
    - "Script (.sh file) triggers approval prompt"
    - "Approve button executes the command and returns output"
    - "Deny button returns denial and Jarvis offers alternatives"
    - "Bot restart recovers pending approvals and notifies user"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete shell execution feature end-to-end via the live Telegram bot: safe commands, blocked commands, risky commands with approval/denial, script execution, and bot restart recovery.

Purpose: All five requirements (SEC-01, SEC-02, SEC-03, EXEC-01, EXEC-02) must be verified as working in the actual Telegram interface before the phase is considered complete.
Output: Human verification that all success criteria pass
</objective>

<execution_context>
@/Users/max/.claude/get-shit-done/workflows/execute-plan.md
@/Users/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-security-shell-execution/02-CONTEXT.md
@.planning/phases/02-security-shell-execution/02-01-SUMMARY.md
@.planning/phases/02-security-shell-execution/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete shell execution flow in Telegram</name>
  <files>N/A — checkpoint only, no files modified</files>
  <action>
    Human verifies all 7 test cases in the live Telegram bot by sending messages to Jarvis and observing responses. No code changes — pure verification.

    Start the bot: `npm run dev`

    **Test 1 — EXEC-01 Safe command (no approval):**
    Send to Jarvis: "Run ls -la in my home directory"
    Expected: Jarvis executes immediately and returns directory listing in a code block. No approval prompt.

    **Test 2 — SEC-01 Blocked command:**
    Send to Jarvis: "Run rm -rf /"
    Expected: Jarvis responds that the command is blocked with an explanation. No execution happens.

    **Test 3 — SEC-02 Risky command with approval:**
    Send to Jarvis: "Run npm list --depth=0"
    Expected: Jarvis sends an approval message with inline keyboard (Aprobar / Denegar). Tap "Aprobar". Command executes and output is returned.

    **Test 4 — SEC-02 Risky command with denial:**
    Send to Jarvis: "Create a file called test.txt in /tmp"
    Expected: Approval prompt appears. Tap "Denegar". Jarvis acknowledges denial and may suggest alternatives.

    **Test 5 — EXEC-02 Script execution:**
    Create a test script: `echo '#!/bin/bash\necho "hello from script"' > /tmp/test-jarvis.sh && chmod +x /tmp/test-jarvis.sh`
    Send to Jarvis: "Run the script at /tmp/test-jarvis.sh"
    Expected: Approval prompt (scripts are always risky). Tap "Aprobar". Output shows "hello from script".

    **Test 6 — SEC-03 Bot restart recovery:**
    Send to Jarvis a risky command to trigger approval prompt. Do NOT tap approve/deny.
    Press Ctrl+C to stop the bot, then restart with `npm run dev`.
    Expected: After restart, Jarvis sends a message saying the pending approval expired during restart.

    **Test 7 — Shell metacharacters rejected:**
    Send to Jarvis: "Run echo hello | cat"
    Expected: Jarvis reports that shell metacharacters are not allowed and suggests separate tool calls.
  </action>
  <read_first>
    - src/tools/built-in/execute-command.ts
    - src/channels/telegram.ts
    - src/index.ts
  </read_first>
  <verify>Human confirms all 7 test cases pass in the live Telegram bot</verify>
  <acceptance_criteria>
    - Test 1: ls output visible in Telegram, no approval prompt shown
    - Test 2: Block message visible, no command execution
    - Test 3: Inline keyboard shown, tapping Aprobar returns npm output
    - Test 4: Inline keyboard shown, tapping Denegar returns denial message
    - Test 5: Script approval + "hello from script" output visible
    - Test 6: After restart, expired approval notification sent
    - Test 7: Metacharacter rejection message shown
  </acceptance_criteria>
  <done>All 7 test cases pass. Phase 2 requirements SEC-01, SEC-02, SEC-03, EXEC-01, EXEC-02 verified end-to-end.</done>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed</resume-signal>
</task>

</tasks>

<verification>
All 7 test cases pass in the live Telegram bot.
</verification>

<success_criteria>
- EXEC-01: Safe shell commands execute and return output (Test 1)
- EXEC-02: Scripts execute with approval (Test 5)
- SEC-01: Destructive commands blocked with explanation (Test 2)
- SEC-02: Risky commands trigger inline keyboard, approve executes, deny stops (Tests 3, 4)
- SEC-03: Pending approvals survive restart (Test 6)
- Shell metacharacters rejected (Test 7)
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-shell-execution/02-03-SUMMARY.md`
</output>
