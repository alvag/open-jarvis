---
phase: 04-supervisor-improvements
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - SUP-01
  - SUP-02
  - SUP-03
  - SUP-04

must_haves:
  truths:
    - "Bot starts and runs under supervisor without errors"
    - "Supervisor writes lifecycle events to data/supervisor.log"
    - "Heartbeat keeps bot alive; stopping heartbeat triggers hang detection and restart"
    - "Graceful shutdown waits for in-flight operations before exiting"
  artifacts: []
  key_links: []
---

<objective>
Verify all four supervisor improvements work end-to-end via live Telegram bot testing.

Purpose: SUP-01 through SUP-04 are process lifecycle behaviors that cannot be unit tested. This plan verifies them by running the actual supervisor and observing behavior in Telegram and logs.

Output: Verified working supervisor with all four improvements confirmed.
</objective>

<execution_context>
@/Users/max/.claude/get-shit-done/workflows/execute-plan.md
@/Users/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-supervisor-improvements/04-CONTEXT.md
@.planning/phases/04-supervisor-improvements/04-01-SUMMARY.md
@.planning/phases/04-supervisor-improvements/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run typecheck and build to validate implementation</name>
  <files></files>
  <read_first>
    - src/supervisor.ts (verify final state)
    - src/index.ts (verify final state)
  </read_first>
  <action>
Run the following commands to validate the implementation compiles:

1. `npm run typecheck` — must exit 0, no type errors
2. `npm run build` — must exit 0, compiles to dist/

If either fails, read the error output and fix the issue in the relevant file (src/supervisor.ts or src/index.ts). Common issues:
- Missing import for `getPendingRestart` in index.ts
- Type mismatch on IPC message shape
- Missing `ChildProcess` type import in supervisor.ts
  </action>
  <verify>
    <automated>cd /Users/max/Personal/repos/open-jarvis && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run typecheck exits with code 0
    - npm run build exits with code 0
    - dist/ directory contains compiled JavaScript files
  </acceptance_criteria>
  <done>
    Both typecheck and build pass with zero errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of supervisor improvements</name>
  <files></files>
  <action>
Present the following verification tests to the user for manual execution. These are process lifecycle behaviors that require running the actual supervisor and observing real Telegram interactions.

**Test 1 — Lifecycle logging (SUP-04):**
1. Start the bot: `npm start` (runs supervisor)
2. Wait for bot to come online (Telegram message "Jarvis esta online")
3. Run: `cat data/supervisor.log`
4. Expected: Log entries with format `[timestamp] [INFO] [supervisor] Starting bot process...`

**Test 2 — Heartbeat is running (SUP-02):**
1. Bot should be running from Test 1
2. Send any message to Jarvis in Telegram — confirm it responds (bot is alive and heartbeat is keeping it alive)
3. The supervisor should NOT restart the bot (no "hung" messages in supervisor.log)
4. Run: `cat data/supervisor.log` — confirm no watchdog warnings

**Test 3 — Graceful shutdown (SUP-01):**
1. Send a message to Jarvis (something that takes a few seconds, like "search the web for latest tech news")
2. While Jarvis is processing, press Ctrl+C in the terminal
3. Expected: You should see "Shutting down Jarvis..." and "Waiting for N in-flight agent run(s)..." in the output
4. Expected: Jarvis should finish the response before shutting down (or timeout after 15s)
5. Expected: Telegram receives "Jarvis reiniciando..." notification

**Test 4 — Supervisor log completeness (SUP-04):**
1. Run: `cat data/supervisor.log`
2. Expected: Contains entries for start, any crashes that occurred, and the shutdown from Test 3

**Test 5 — Auto-update detection (SUP-03):**
1. This is harder to test live — verify by reading the code
2. Confirm `pollForUpdates` function exists and is called on a 5-minute interval
3. Run: `grep "pollForUpdates" src/supervisor.ts` — should return matches
  </action>
  <verify>User confirms all tests pass by typing "approved"</verify>
  <done>All four SUP requirements verified via live testing and log inspection. Human typed "approved".</done>
</task>

</tasks>

<verification>
All four SUP requirements verified via live testing and log inspection.
</verification>

<success_criteria>
Human confirms: bot starts cleanly under supervisor, lifecycle events appear in data/supervisor.log, heartbeat keeps bot alive, graceful shutdown waits for in-flight operations, and auto-update polling code is present and structured correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-supervisor-improvements/04-03-SUMMARY.md`
</output>
